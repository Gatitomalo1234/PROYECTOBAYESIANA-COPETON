\documentclass[12pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[spanish]{babel}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{booktabs}
\usepackage{geometry}
\usepackage{hyperref}
\usepackage{enumitem}
\usepackage{graphicx}
\usepackage{float}

\geometry{margin=1in}

\title{Diseño Metodológico: Impacto de la Contaminación en la Avifauna Urbana}
\author{Nicolas Cardenas, Miguel Camargo, Camilo Hernandez}
\date{}

\begin{document}

\maketitle

\section{Parte 1 --- Primera Entrega: Pensamiento Crítico}

\subsection*{Análisis Bibliográfico}

\textbf{1. Enunciar el algoritmo de búsqueda en Web of Science (WOS)}

Para capturar la intersección de modelos probabilísticos, ecología urbana y calidad del aire en la base de datos de Web of Science (WOS), el algoritmo (cadena de búsqueda booleana) utilizado es:
\begin{verbatim}
TS=(("occupancy model*" OR "Bayesian occupancy" OR "hierarchical model*") 
AND ("bird*" OR "avian" OR "Zonotrichia capensis") 
AND ("air pollution" OR "particulate matter" OR "urban noise" OR "urbanization"))
\end{verbatim}
\textit{Filtros aplicados}: Documentos de los últimos 15 años (2009-2024), categorías de ``Ecology'', ``Environmental Sciences'', y ``Ornithology''.

\vspace{0.5cm}
\textbf{2. Análisis Bibliométrico}
\begin{itemize}[leftmargin=*]
    \item \textbf{Keywords (Palabras Clave):} Las palabras más recurrentes en la literatura extraída conforman tres clústeres: (1) \textit{Imperfect detection}, \textit{Hierarchical modeling}, \textit{Bayesian inference}; (2) \textit{Urban ecology}, \textit{Anthropogenic noise}, \textit{Air quality}; (3) \textit{Avian community}, \textit{Neotropical birds}.
    \item \textbf{Autores Destacados:} Destacan autores metodológicos como J.A. Royle y D.I. MacKenzie (pioneros en Occupancy Models). En el nicho de impactos urbanos en aves neotropicales y ruido, I. MacGregor-Fors y C.P. Silva. En metodologías computacionales (Logit Link Bayesian), D.S. Johnson y J.S. Clark.
    \item \textbf{Universidades e Instituciones:} Cornell University (Cornell Lab of Ornithology debido al uso de eBird), Universidad Nacional Autónoma de México (UNAM), University of Washington, y centros como USGS Patuxent Wildlife Research Center.
\end{itemize}

\subsection*{Identificación de Bibliografía Clave: Resumen de 5 Artículos}

\textbf{1. Ortega-Álvarez, R., \& MacGregor-Fors, I. (2011). \textit{Effects of traffic noise on occupancy patterns of forest birds.}}
\begin{itemize}[leftmargin=0.5cm]
    \item \textbf{Objetivo:} Evaluar cómo el ruido del tráfico altera la probabilidad de ocupación de las especies de aves en un ecosistema urbano.
    \item \textbf{Temática Teórica:} Ecología urbana; contaminación acústica como filtro ambiental; modelos de ocupación.
    \item \textbf{Metodología:} Muestreo de puntos de conteo repetidos utilizando covariables de detección (tiempo del día) y ambientales en una matriz de historial ($1/0$).
    \item \textbf{Resultados:} El ruido reduce drásticamente la ocupación, demostrando que la contaminación empuja a las poblaciones a excluirse funcionalmente del hábitat.
\end{itemize}

\textbf{2. Silva, C. P., et al. (2022). \textit{Urban noise and surrounding city morphology influence green space occupancy by native birds...}}
\begin{itemize}[leftmargin=0.5cm]
    \item \textbf{Objetivo:} Establecer el rol del paisaje y la perturbación en la ocupación de aves nativas.
    \item \textbf{Temática Teórica:} Respuestas funcionales de aves neotropicales y efectos de borde antrópicos.
    \item \textbf{Metodología:} Modelado jerárquico de ocupación que separa la probabilidad de detección real de la usencia utilizando variables métricas urbanas y datos acústicos.
    \item \textbf{Resultados:} Factores de estrés urbano como barreras de polución y ruido disminuyen la permanencia de especies en áreas urbanas.
\end{itemize}

\textbf{3. Johnson, D. S., et al. (2013). \textit{Efficient Bayesian analysis of occupancy models with logit link functions.}}
\begin{itemize}[leftmargin=0.5cm]
    \item \textbf{Objetivo:} Formular una metodología computacional iterativa rápida (Gibbs Sampling) para modelos de ocupación logísticos a través de variables latentes.
    \item \textbf{Temática Teórica:} Estadística Bayesiana, MCMC, variables de Pólya-Gamma.
    \item \textbf{Metodología:} Conversión de la distribución de verosimilitud Bernoulli/Logit a aproximaciones condicionales normales gaussianas para inferencia analítica.
    \item \textbf{Resultados:} Implementación que agiliza exponencialmente el ajuste de modelos complejos sobre datasets masivos.
\end{itemize}

\textbf{4. Dugger, K. M., et al. (2021). \textit{Wildfire smoke affects detection of birds in Washington State.}}
\begin{itemize}[leftmargin=0.5cm]
    \item \textbf{Objetivo:} Investigar si la extrema contaminación por aerosol altera la probabilidad de detección ($p$) frente a la de ocupación ($\psi$).
    \item \textbf{Temática Teórica:} Interrelación material particulado ($\text{PM}_{2.5}$) y comportamiento animal aviar.
    \item \textbf{Metodología:} Cruce espacial de datos de biomonitoreo con concentración atmosférica satelital y modelos Bayesianos.
    \item \textbf{Resultados:} La contaminación suprime primero el canto y actividad biológica (afectando la detección matemática) haciendo vital controlar los modelos por calidad del aire.
\end{itemize}

\textbf{5. Sanderfoot, O. V., et al. (2017). \textit{A review of the effects of air pollution on birds.}}
\begin{itemize}[leftmargin=0.5cm]
    \item \textbf{Objetivo:} Sintetizar la toxicología aviar y cómo el aparato respiratorio de las aves reacciona ante estrés químico urbano.
    \item \textbf{Temática Teórica:} Gases pesados ($\text{SO}_2, \text{NO}_2$) y partículas en suspensión ambientales.
    \item \textbf{Metodología:} Revisión sistemática ecofisiológica.
    \item \textbf{Resultados:} La anatomía aviar de flujo unidireccional maximiza la absorción de toxinas, lo que vuelve a la polución gaseosa un predictor determinante de la ausencia poblacional en ciertas matrices geográficas.
\end{itemize}


\section{Parte 2 --- Planteamiento Metodológico}

\subsection*{1. Planteamiento del problema}
La ciudad de Bogotá enfrenta desafíos asociados a la contaminación atmosférica. Si bien sus efectos sobre la salud pública están muy documentados, existe la necesidad de evaluar de forma cuantitativa cómo esta problemática ambiental afecta a la biodiversidad urbana nativa, como el copetón (\textit{Zonotrichia capensis}). Estudiar esta relación requiere cruzar datos de plataformas de ciencia ciudadana (eBird) con registros de monitoreo ambiental (RMCAB), lidiando con la incertidumbre y la naturaleza de datos de observación que no provienen de diseños experimentales controlados.

\subsection*{2. Pregunta de investigación}
¿Cómo afectan los niveles de contaminación atmosférica urbana a la probabilidad de observar al copetón (\textit{Zonotrichia capensis}) en la ciudad de Bogotá?

\subsection*{3. Justificación}
Este proyecto se justifica empíricamente por la oportunidad de utilizar dos grandes fuentes de datos abiertos (eBird y la Red de Monitoreo de Calidad del Aire de Bogotá) para analizar el posible rol del copetón como bioindicador de la calidad ambiental urbana. Metodológicamente, el uso de una regresión logística en un marco bayesiano es pertinente ya que permite cuantificar la relación entre la presencia del ave y los contaminantes atmosféricos asumiendo de manera explícita la incertidumbre en las estimaciones a través de distribuciones posteriores.

\subsection*{4. Establezca un objetivo general y específicos}

\textbf{Objetivo General:}\\
Analizar la asociación estadística entre los niveles de contaminación atmosférica urbana y la probabilidad de registrar al copetón (\textit{Zonotrichia capensis}) en Bogotá, mediante la implementación matemática de una regresión logística bayesiana.

\textbf{Objetivos Específicos:}
\begin{enumerate}
    \item Consolidar una base de datos que integre geográficamente y espacialmente los registros de observación de aves (eBird) con las mediciones horarias de calidad del aire (RMCAB).
    \item Estimar la probabilidad de observar al copetón analizando las covariables de material particulado ($\text{PM}_{10}, \text{PM}_{2.5}$) y gases, incluyendo variables de control sobre el esfuerzo de observación.
    \item Cuantificar la incertidumbre de los parámetros estimados mediante la obtención de sus distribuciones posteriores.
\end{enumerate}

\subsection*{5. Paso a paso de la metodología}

Para que cualquier persona pueda replicar exactamente este trabajo desde cero, la metodología sigue estos 4 pasos directos y secuenciales:

\begin{enumerate}
    \item \textbf{Obtención de bases de datos:} Primero, se extraen los registros horarios de la calidad del aire provistos por OpenAQ y la RMCAB. Para el caso de los avistamientos, es necesario realizar una solicitud formal a través de correo electrónico al portal de eBird para que aprueben la descarga de la base de datos histórica completa de la ciudad.
    \item \textbf{Cruce espaciotemporal:} Mediante un script en Python, se une cada avistamiento de un ave con la contaminación registrada en esa misma hora. Las coordenadas del observador se emparejan con la estación de calidad del aire más cercana utilizando fórmulas geométricas de distancia.
    \item \textbf{Filtro biológico y limpieza:} De todo el universo de datos cruzados, se eliminan las listas pobres y nos quedamos exclusivamente con las "listas completas" de eBird. Esto es vital para asegurar que la no-detección de un copetón sea matemáticamente una ausencia real y no un error humano. Además, se controlan estadísticamente variables como la distancia recorrida o el tiempo invertido buscando.
    \item \textbf{Modelado Bayesiano:} Finalmente, la base de datos consolidada se ingresa al algoritmo. Se ejecuta una regresión logística bayesiana para calcular si las curvas de material particulado y gases alteran la ocurrencia del copetón, permitiendo que el modelo capture toda la incertidumbre en sus distribuciones posteriores.
\end{enumerate}
\section{Parte 3 --- Análisis Exploratorio de Datos (EDA)}

Esta sección detalla el comportamiento estadístico y distribucional de las base de datos combinada (eBird y RMCAB).

\subsection*{1. Resumen de Estadísticas e Imputación de Nulos}

\textbf{Diccionario de Variables:}\\
El dataset consolidado (\texttt{copeton\_occupancy\_ready.csv}) cuenta con 910 unidades de muestreo compuestas por las siguientes variables predictoras:
\begin{itemize}
    \item \textbf{Variable Respuesta (Latente):} \textit{y\_copeton} (1/0 Detección positiva de la especie).
    \item \textbf{Variables Ambientales (RMCAB):} \textit{pm10\_ugm3} ($\mu g/m^3$), \textit{pm25\_ugm3} ($\mu g/m^3$), \textit{co\_ppm} (Concentración CO), \textit{no2\_ppb} (Concentración $NO_2$), \textit{o3\_ppb} (Ozono), \textit{so2\_ugm3} (Dióxido de Azufre).
    \item \textbf{Variables de Detección Densa (eBird):} \textit{DURATION MINUTES} (Esfuerzo temporal invertido), \textit{EFFORT DISTANCE KM} (Distancia caminada), \textit{NUMBER OBSERVERS} (Tamaño del grupo pajarero), \textit{PROTOCOL NAME} (Traveling vs Stationary).
    \item \textbf{Variables Espacio-Temporales:} \textit{nearest\_station} (Estación climatológica cruzada geométricamente), \textit{month} (Control de estacionalidad mensual).
\end{itemize}

\vspace{0.3cm}
\textbf{Completitud e Imputación:}\\
Un paso fundamental antes del modelado fue analizar la pureza de los datos recopilados por las 14 estaciones de Bogotá (2021-2026). Al cruzar los registros aviares con las mediciones atmosféricas, descubrimos una discrepancia instrumental significativa:
\begin{itemize}
    \item \textbf{Alta Completitud:} El Material Particulado ($PM_{10}$ y $PM_{2.5}$) gozan de una completitud excelente con solo $1.76\%$ y $0.55\%$ de nulos, respectivamente.
    \item \textbf{Déficit Severo:} Los gases como el Dióxido de Nitrógeno ($NO_2$) y el Monóxido de Carbono ($CO$) sufrieron ausencias extremas, bordeando casi un $40\%$ y $46\%$ de nulos espaciales. El Ozono ($O_3$) mantuvo un nivel moderado ($12\%$ de nulos).
\end{itemize}
Basado en estos descriptivos genéricos, determinamos que era estocásticamente inviable basar un modelo en variables gaseosas como el $NO_2$. Rellenar de forma adivinatoria casi el $40\%$ de una variable alteraría severamente las conclusiones poblacionales arrojando varianzas artificialmente compactas. 

\subsection*{2. Gráficos de Análisis Exploratorio: Histogramas y Densidad}
A continuación, se presentan las curvas de probabilidad (Kernel Density Estimation - KDE) cruzadas con el histograma para el inventario completo de predictores, abarcando tanto contaminantes gaseosos como el esfuerzo metodológico del investigador.

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{plots/density_plots_all.png}
    \caption{Curvas de densidad para el espectro completo de las covariables (Gases, Partículas y Esfuerzo de Muestreo).}
    \label{fig:kde_all}
\end{figure}

Al analizar la Figura \ref{fig:kde_all}, es claramente visible que ninguna de estas variables sigue la famosa Campana de Gauss perfectamente simétrica. Por el contrario, muestran un sesgo hacia la izquierda, estirando una larga cola de datos hacia la derecha. \\
Esto tiene todo el sentido ecológico: en Bogotá la mayoría de los días presentan una aglomeración de mediciones estables y bajas de polución, con esporádicos eventos anómalos (como picos industriales o incendios) graficados en los extremos derechos. Matemáticamente, constatar esta irregularidad nos exige descartar pruebas asimiladas a modelos asintóticos y nos justifica plenamente el uso de métodos probabilísticos más robustos capaces de dominar el ruido y trabajar con distribuciones inclinadas.

\subsection*{3. Matriz de Correlaciones de las Variables}
Debido a la ausencia de normalidad evidenciada en los gráficos previos, se generó una matriz de rango de Spearman ($\rho$) abarcando todos los vectores para sondear posibles colinealidades.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{plots/correlation_matrix.png}
    \caption{Matriz de Correlación de rangos de Spearman ($\rho$).}
    \label{fig:corrmatrix}
\end{figure}

El diagnóstico principal que arroja la matriz es la absoluta independencia cruzada de la mayoría de gases y tiempos de muestreo. Sin embargo, sobresale ineludiblemente una \textbf{Multicolinealidad Fuerte} ($\rho = 0.77$) entre el $PM_{10}$ y el $PM_{2.5}$. En consecuencia a este hallazgo paramétrico, dictaminamos seleccionar como variable primaria únicamente al $PM_{10}$, dado que engloba un polvo suspendido a niveles de arbusto rastrojero bajo, capturando con mayor fidelidad las perturbaciones del estrato del Copetón, a diferencia de los gases de alta suspensión.

\subsection*{4. Pruebas de Hipótesis (Por qué la estadística básica falla)}
Aplicamos una prueba matemática (Mann-Whitney) separando el análisis en dos bloques estratégicos: el biológico (el efecto ambiental puro) y el metodológico (el esfuerzo humano subyacente).

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{plots/pvalues_mannwhitney_bio.png}
    \caption{Valores-P para el Componente Biológico de Contaminantes.}
    \label{fig:pvalues_bio}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{plots/pvalues_mannwhitney_method.png}
    \caption{Valores-P para el Componente Metodológico de Esfuerzo Humano.}
    \label{fig:pvalues_method}
\end{figure}

Los resultados en cadena revelan algo sumamente interesante. A simple vista, si solo miramos la Figura \ref{fig:pvalues_bio}, partículas agresivas como el material particulado ($PM_{10}$) parecen fallar la prueba, dando la aparente conclusión falsa de que no tienen importancia en el ecosistema. Sin embargo, al observar el componente humano (Figura \ref{fig:pvalues_method}), las horas invertidas por el observador rompen absolutamente la prueba marcando una importancia monstruosa ($p < 0.0001$). Curiosamente, la distancia física caminada resultó ser estadísticamente irrelevante ($p=0.96$). Esto tiene todo el sentido en ornitología: para detectar a este pequeño pájaro rastrojero importa muchísimo más la paciencia de "cuánto tiempo te quedas observando un punto", que el puro esfuerzo físico de "cuántos kilómetros caminas rápido".

Finalmente, evaluamos el componente categórico del espacio físico y la temporalidad mediante una prueba de \textbf{Chi-Cuadrado de Pearson} ($\chi^2$).

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{plots/pvalues_chisquare.png}
    \caption{Valores-P de Chi-Cuadrado para Variables Cualitativas y Geográficas.}
    \label{fig:pvalues_chi2}
\end{figure}

El resultado de la Figura \ref{fig:pvalues_chi2} es rotundo: la Estación meteorológica más cercana a la observación arroja un valor colosal ($p<0.0001$). Esto certifica probabilísticamente que censar al lado del humedal de Kennedy te dará posibilidades ecológicas radicalmente opuestas a censar parado en una acera industrial, lo que nos obliga formalmente a usar la ubicación como control en nuestro modelo iterativo probabilístico.

\vspace{0.5cm}
\subsection*{Conclusión Final del EDA}
Culminada la exploración visual y probabilística de la base de datos, el diseño del inminente Modelo Bayesiano exigirá la siguiente arquitectura predictora, sustentada estrictamente en nuestro filtraje matemático:

\textbf{Lo que descartamos:}
\begin{itemize}
    \item \textbf{Gases ($NO_2$, $SO_2$, $CO$):} Descartados para no inducir falsas certezas en el algoritmo probabilístico, dado el alto déficit de sus mediciones ($\sim 40\%$ de vacíos de información).
    \item \textbf{Material Particulado Fino ($PM_{2.5}$):} Sacrificado al comprobarse como una variable redundante y clonada frente a la dispersión de partículas gruesas (Correlación de Spearman de $0.77$ frente al $PM_{10}$).
\end{itemize}

\textbf{Factores Biológicos de Ocupación:}
\begin{itemize}
    \item \textbf{Material Particulado Grueso ($PM_{10}$):} Seleccionado por idoneidad ecosistémica. Representa el polvo pesado decantado sobre el césped, modelando con fidelidad la disrupción en el estrato bajo de forrajeo del ave.
    \item \textbf{Ozono ($O_3$):} Integrado a la ecuación biológica tras ser el único gas capaz de probar un estrés estadísticamente significativo sobre nuestra población ($p < 0.05$).
\end{itemize}

\textbf{Componentes de Esfuerzo Muestral:}
\begin{itemize}
    \item \textbf{Minutos de Duración:} Anclado al modelo obligatoriamente ($p < 0.0001$). Al seleccionarlo descartamos definitivamente a la Distancia Caminada ($p=0.96$), garantizando que el éxito de reportar a esta ave dependió del tiempo invertido y no de los kilómetros recorridos.
    \item \textbf{Estación y Protocolo:} Factores geográficos y metodológicos adheridos de forma obligatoria tras sus colosales pruebas de Chi-Cuadrado ($p < 0.0001$), neutralizando así el grave sesgo visual humano que destrozaría cualquier modelo básico.
\end{itemize}


% ============================================================
\section{Parte 4 --- Diseño del Modelo Bayesiano de Ocupación}
% ============================================================

Culminado el análisis exploratorio, esta sección formaliza la arquitectura matemática del modelo que procesará los datos consolidados. Se trata de un \textbf{Modelo Jerárquico Bayesiano de Ocupación} que separa el estado ecológico real del ave (ocupación latente $\psi$) del error de observación humana (detección $p$), siguiendo la formulación eficiente de Clark \& Altwegg (2019) con aumento de datos de Pólya-Gamma.

\subsection*{4.1 Fundamento: El Teorema de Bayes}

La meta del análisis es hallar la \textbf{Distribución Posterior} de los parámetros ($\theta$), dado que hemos observado ciertos datos ($D$):

\begin{equation}
  P(\theta \mid D) = \frac{P(D \mid \theta)\, P(\theta)}{P(D)}
\end{equation}

Donde:
\begin{itemize}
  \item $P(D \mid \theta)$ es la \textbf{Likelihood} (Verosimilitud): ¿Qué tan probables son los datos según nuestros parámetros?
  \item $P(\theta)$ es la \textbf{Prior}: Nuestra suposición inicial sobre los parámetros antes de observar los datos.
  \item $P(D)$ es la \textit{evidencia}, una constante de normalización que se ignora en el muestreo MCMC.
\end{itemize}

\subsection*{4.2 Separación de Parámetros: Beta vs. Alpha}

En los modelos jerárquicos de ocupación se independiza matemáticamente el ecosistema real del error humano de observación. Por convención, el modelo se fractura en dos mundos que nunca se confunden:

\begin{itemize}
  \item \textbf{Mundo Ecológico --- Parámetros Beta ($\beta$):} Modelan exclusivamente la \textbf{Biología} de la especie (Ocupación latente $\psi$). Responden a la pregunta: \textit{¿El ecosistema permite que el ave viva en este sitio?} Cuentan con un intercepto ecológico base ($\beta_0$).
  \item \textbf{Mundo Humano --- Parámetros Alpha ($\alpha$):} Modelan exclusivamente el \textbf{Error Observacional} (Detección $p$). Responden a la pregunta: \textit{Asumiendo que el ave sí estaba presente, ¿el pajarero logró registrarla?} Cuentan con un intercepto de detectabilidad base ($\alpha_0$).
\end{itemize}

Esta separación es fundamental: si usáramos los mismos símbolos para ambos ejes, un evaluador podría confundir el efecto del protocolo de observación (variable humana) con un supuesto efecto biológico sobre la especie. Los $\beta$ pertenecen a la ecología; los $\alpha$ al error humano.

\subsection*{4.3 Especificación de las Priors (No Informativas)}

A partir del EDA realizado, los hiperparámetros se configuran sobre dos componentes aislados:

\begin{itemize}
  \item \textbf{Ecuación de Ocupación Ecológica ($\psi$):} Estructurada por $\beta_0$ (intercepto base), $\beta_{\text{PM10}}$ (efecto del $\text{PM}_{10}$) y $\beta_{\text{O3}}$ (efecto del Ozono).
  \item \textbf{Ecuación de Detección/Esfuerzo ($p$):} Estructurada por $\alpha_0$ (intercepto base), $\alpha_{\text{duracion}}$ (tiempo en minutos), $\alpha_{\text{estacion}}$ (heterogeneidad geográfica) y $\alpha_{\text{protocolo}}$.
\end{itemize}

Para acoplarse al aumento de variables de Pólya-Gamma y a la función logística (Logit), todas las pre-creencias sobre los coeficientes se modelan con \textbf{Distribuciones Normales} no informativas. Cada coeficiente distribuye de manera independiente:

\medskip
\textbf{Eje Ecológico ($\psi$):}
\begin{align}
  \beta_0 &\sim \mathcal{N}(0,\, 1) \\
  \beta_{\text{PM10}} &\sim \mathcal{N}(0,\, 1) \\
  \beta_{\text{O3}} &\sim \mathcal{N}(0,\, 1)
\end{align}

\textbf{Eje de Detección ($p$):}
\begin{align}
  \alpha_0 &\sim \mathcal{N}(0,\, 1) \\
  \alpha_{\text{duracion}} &\sim \mathcal{N}(0,\, 1) \\
  \alpha_{\text{estacion}} &\sim \mathcal{N}(0,\, 1) \\
  \alpha_{\text{protocolo}} &\sim \mathcal{N}(0,\, 1)
\end{align}

\textbf{Eje Latente de Acondicionamiento ($\omega$):}
\begin{equation}
  \omega \sim \mathcal{PG}(b=1,\; c=0)
\end{equation}

La distribución Pólya-Gamma (Polson et al., 2013) fija $b=1$ al estar condicionada a la verosimilitud binaria de eBird, y ancla $c=0$ para que su media sea neutra, comportándose como un acondicionador universal.

\subsubsection*{Justificación Metodológica de la Prior No Informativa}

El uso estricto de una prior neutra $\mathcal{N}(0, 1)$ se fundamenta en una \textbf{premisa de objetividad por falta de experticia a priori}:

\begin{itemize}
  \item Como equipo, no somos ornitólogos expertos como para asegurar puntualmente qué tanto un microgramo de Ozono diezma a las poblaciones de \textit{Z. capensis}. Al no saberlo, nos declaramos ignorantes fijando la expectativa central en cero ($\mu_0 = 0$).
  \item Modificar la media o la varianza para ``ajustar'' el modelo, aunque fuera en un mísero $0.01$, implicaría insertar conocimiento preconcebido, sesgando irreparablemente el diseño algorítmico.
  \item Si el modelo decreta que la polución impacta la ocupación, ese descubrimiento proviene \textbf{única y exclusivamente} de la presión de los datos de eBird sobre nuestra hoja en blanco empírica, no de un juicio personal embebido en el código.
\end{itemize}

\subsection*{4.4 La Likelihood (Verosimilitud): De la Bernoulli a la Pólya-Gamma}

Entender de dónde sale la verosimilitud final es la clave para defender la metodología. El proceso sigue una lógica de tres escalones:

\subsubsection*{Paso 1: La Naturaleza Binaria (Bernoulli)}

En cada evento de observación, el resultado es binario: ``vi al Copetón (1)'' o ``no lo vi (0)''. La verosimilitud de un solo dato $y_i$ (presencia/ausencia) se define como:

\begin{equation}
  P(y_i \mid \psi_i) = \psi_i^{\,y_i}\,(1 - \psi_i)^{1-y_i}
\end{equation}

\subsubsection*{Paso 2: El Enlace Logístico (Inverse-Logit)}

Para vincular la probabilidad biológica $\psi_i$ con las variables ambientales $X_i\beta$, se usa la función Inverse-Logit, que fuerza cualquier valor real al intervalo $(0,1)$:

\begin{equation}
  \psi_i = \frac{e^{X_i\beta}}{1 + e^{X_i\beta}}
\end{equation}

Al sustituir esta expresión en la Bernoulli del Paso~1, se llega a la expresión computacionalmente difícil:

\begin{equation}
  P(y_i \mid X_i\beta) = \frac{\left(e^{X_i\beta}\right)^{y_i}}{1 + e^{X_i\beta}}
\end{equation}

El denominador $\left(1 + e^{X_i\beta}\right)$ es no lineal. Al multiplicarlo con la Prior Normal, los exponentes no se cancelan limpiamente, por lo que no existe una solución analítica directa (``solución cerrada''). El algoritmo se vería obligado a adivinar la forma de la curva a ciegas, iteración a iteración (Metropolis-Hastings).

\subsubsection*{Paso 3: El Acondicionamiento Estocástico (Identidad de Pólya-Gamma)}

Para romper el denominador intratable, Clark (2019) aplica la identidad de Polson et al.\ (2013). Esta identidad permite reescribir la probabilidad como una integral ponderada por la variable latente $\omega$:

\begin{equation}
  \underbrace{\dfrac{\left(e^{\psi}\right)^y}{1+e^{\psi}}}_{\text{Inverse-Logit}}
  \;=\;
  \underbrace{\dfrac{1}{2}\exp\!\left((y - \tfrac{1}{2})\psi\right)}_{\text{Capa Lineal}}
  \int_0^{\infty}
  \underbrace{\exp\!\left(-\dfrac{\omega\,\psi^2}{2}\right)}_{\text{Capa Cuadrática (Normal)}}
  p(\omega \mid 1,\, 0)\; d\omega
  \label{eq:pg_identity}
\end{equation}

De la ecuación~\eqref{eq:pg_identity} se desprenden dos consecuencias clave:

\begin{itemize}
  \item \textbf{El Transformador Latente ($\omega$):} No es un parámetro fijo, es una \textbf{variable latente estocástica}. En cada iteración se le pregunta a la distribución $\mathcal{PG}(1,0)$: \textit{``¿Qué peso necesito para que este dato de eBird se vea lineal?''}. El valor $\omega_i$ resultante ``endereza'' localmente la curva del Logit.
  \item \textbf{La Linealización:} El término $\exp\!\left(-\tfrac{\omega\psi^2}{2}\right)$ es una \textbf{estructura cuadrática en el exponente}. En estadística, una parábola en el exponente es la firma matemática de una \textbf{Distribución Normal}.
\end{itemize}

Dado que la Prior también es Normal, al tener una Likelihood que se comporta como Normal y una Prior Normal, el producto de ambas produce una \textbf{solución cerrada} (otra Normal). El modelo deja de ``adivinar'' y puede calcular la respuesta de forma directa y exacta.

\medskip
La \textbf{Likelihood Aumentada de Datos Completos} para los $N$ sitios y $K_i$ visitas por sitio queda entonces como:

\begin{equation}
  L\!\left(y, z, \omega \mid \beta, \alpha\right)
  = \prod_{i=1}^N
    \underbrace{P\!\left(z_i,\, \omega^\psi_i \mid \beta\right)}_{\text{Capa Biológica}}
    \cdot
    \prod_{j=1}^{K_i}
    \underbrace{P\!\left(y_{i,j},\, \omega^p_{i,j} \mid z_i,\, \alpha\right)}_{\text{Capa Humana}}
\end{equation}

Donde:
\begin{itemize}
  \item \textbf{Capa Biológica:} $z_i \sim \text{Bernoulli}(\psi_i)$. La contaminación empuja $\psi_i \to 0$.
  \item \textbf{Capa Humana:} $y_{i,j} \sim \text{Bernoulli}(z_i \cdot p_{i,j})$. Si $z_i = 0$ (el ave fue extirpada por el $\text{PM}_{10}$), todo el esfuerzo humano se multiplica por cero ($0 \times p = 0$). No se puede detectar lo que no está presente.
\end{itemize}

\subsection*{4.5 Construcción de la Posterior Paso a Paso}

Aquí es donde se fusiona todo el conocimiento previo con la evidencia de los datos.

\subsubsection*{La Fórmula de la Posterior Conjunta}

Aplicando la regla de Bayes al modelo aumentado, la \textbf{Posterior Conjunta} es proporcional al producto de la Likelihood Aumentada por todas las Priors:

\begin{equation}
  \boxed{
  P\!\left(\beta, \alpha, \mathbf{z}, \boldsymbol{\omega} \mid \mathbf{y}\right)
  \;\propto\;
  \underbrace{L\!\left(\mathbf{y}, \mathbf{z}, \boldsymbol{\omega} \mid \beta, \alpha\right)}_{\text{Datos (Likelihood)}}
  \;\times\;
  \underbrace{\pi(\beta)\;\pi(\alpha)\;\pi(\boldsymbol{\omega})}_{\text{Ignorancia (Priors)}}
  }
\end{equation}

Al sustituir los componentes, se observa la fusión completa:

\begin{multline}
  P\!\left(\beta, \alpha, \mathbf{z}, \boldsymbol{\omega} \mid \mathbf{y}\right)
  \;\propto\;
  \prod_{i=1}^N
  \left[
    P\!\left(z_i, \omega^\psi_i \mid \beta\right)
    \prod_{j=1}^{K_i}
    P\!\left(y_{i,j}, \omega^p_{i,j} \mid z_i, \alpha\right)
  \right] \\
  \times\;
  \mathcal{N}(\beta \mid 0, 1)
  \;\times\;
  \mathcal{N}(\alpha \mid 0, 1)
  \;\times\;
  \mathcal{PG}(\omega \mid 1, 0)
\end{multline}

\subsubsection*{La Solución por Bloques: Muestreo de Gibbs (Gibbs Sampling)}

Como no es posible resolver la ecuación conjunta de una sola vez, el modelo la rompe en \textbf{4 bloques iterativos} que se actualizan en ciclo:

\begin{enumerate}
  \item \textbf{Bloque $z$ (Estado Latente):} Se imputa el estado real del ave en los sitios donde no hubo detección, comparando la probabilidad de extirpación ecológica contra la de fallo observacional del pajarero.
  \item \textbf{Bloque $\omega$ (Acondicionamiento):} Se generan las variables latentes Pólya-Gamma que aplanan el Logit para convertirlo en una distribución Normal.
  \item \textbf{Bloque $\beta$ (Ecología):} Se calculan los efectos del $\text{PM}_{10}$ y el $\text{O}_3$. Gracias a $\omega$, este paso tiene una distribución \textbf{Normal} de solución directa.
  \item \textbf{Bloque $\alpha$ (Humano):} Se calculan los efectos del esfuerzo del pajarero (duración, protocolo, estación). Igualmente resulta en una distribución \textbf{Normal} de solución directa.
\end{enumerate}

\subsubsection*{La Mecánica de la Solución Cerrada (Conjugación Normal-Normal)}

En cada iteración de los Bloques 3 y 4, la Posterior de los coeficientes se calcula mediante conjugación \textbf{Normal--Normal}. Las fórmulas de actualización son:

\medskip
\textbf{1. Varianza Posterior Actualizada ($V_n$):}
\begin{equation}
  V_n = \left(X^T \Omega\, X + V_0^{-1}\right)^{-1}
\end{equation}

donde $\Omega = \text{diag}(\omega_1, \dots, \omega_N)$ es la matriz diagonal de los pesos fantasmas y $V_0^{-1}$ es la precisión de la Prior (para $\mathcal{N}(0,1)$, $V_0^{-1} = I$).

\medskip
\textbf{2. Media Posterior Actualizada ($\mu_n$):}
\begin{equation}
  \mu_n = V_n \left(X^T \kappa + V_0^{-1}\,\mu_0\right)
\end{equation}

donde $\kappa_i = y_i - \tfrac{1}{2}$ son los datos de eBird centrados y $\mu_0 = \mathbf{0}$ es nuestra media inicial de ignorancia.

\medskip
Este mecanismo convierte un problema de optimización no lineal y lento en una serie de \textbf{operaciones algebraicas directas y exactas}, garantizando la convergencia eficiente del modelo.

\subsubsection*{La Convergencia de las Cadenas de Markov}

El ciclo de 4 bloques se repite miles de veces. Al principio, los valores saltan erráticamente (período de \textit{burn-in}, ${\sim}1{,}000$ iteraciones, se descartan). Tras la convergencia, los siguientes ${\sim}10{,}000$ valores constituyen la muestra empírica de la Posterior: la respuesta final del modelo.

\subsection*{4.6 Resultado Final: Los Parámetros Posteriores}

Al terminar el proceso, el modelo entrega miles de muestras de la Posterior. Para cada contaminante se obtienen:

\begin{enumerate}
  \item \textbf{Estimación puntual:} La media o mediana de la distribución posterior.
  \item \textbf{Incertidumbre cuantificada:} Intervalos de Credibilidad al 95\%.
\end{enumerate}

\medskip
\textbf{Interpretación Biológica:}
Si el intervalo de credibilidad al 95\% del coeficiente $\beta_{\text{PM10}}$ no incluye al cero y es completamente negativo:

\begin{equation}
  \text{IC}_{95\%}[\beta_{\text{PM10}}] < 0
\end{equation}

habremos demostrado que el $\text{PM}_{10}$ reduce significativamente la probabilidad de ocupación del Copetón (\textit{Zonotrichia capensis}) en Bogotá, \textbf{habiendo controlado todo el ruido de imperfección de detección humana} a través de la capa jerárquica de los parámetros $\alpha$.

\vfill
\noindent\rule{\textwidth}{0.4pt}
\small\textit{Referencias metodológicas: Clark \& Altwegg (2019). Efficient Bayesian analysis of occupancy models with logit link functions. \textbf{Ecology and Evolution}. Polson, Scott \& Windle (2013). Bayesian Inference for Logistic Models Using Pólya-Gamma Latent Variables. \textbf{Journal of the American Statistical Association}.}

\end{document}